version: '3.9'

networks:
  p3o:
    external: true

services:
  postgres:
    container_name: postgres
    image: docker.iranserver.com/postgres:17
    environment:
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
    volumes:
      - postgres_p3o_data:/var/lib/postgresql/data
    networks:
      - p3o
    ports:
      - "5432:5432"
    restart: always

  redis:
    container_name: redis
    image: docker.iranserver.com/bitnami/redis:latest
    command: redis-server --requirepass ${REDIS_PASSWORD}
    environment:
      - ALLOW_EMPTY_PASSWORD=no
    volumes:
      - redis_p3o_data:/var/lib/redis
    networks:
      - p3o
    ports:
      - "6379:6379"
    restart: always

  p3o:
    container_name: p3o
    image: docker.iranserver.com/gatec/p3o:${VERSION}
    volumes:
      - /srv/configs/p3o/app.yaml:/app/configs/config.yaml
    networks:
      - p3o
    depends_on:
      - postgres
    restart: always

volumes:
  postgres_p3o_data:
  redis_p3o_data:
